package com.example.hr_system.repository;

import com.example.hr_system.entities.ListSupport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ListSupportRepository extends JpaRepository<ListSupport, Long> {
}
